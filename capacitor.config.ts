import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.final.665',
  appName: 'final',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
